#include <bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int>v;  //type 1;

    // vector<int> v(10);  //type 2;

    // vector<int> v(15, -5);  //type 3;
    // for(int x:v)
    // cout<<x<<endl;

    // vector<int> v2(v);  //type 4;
    // for(int x:v2)
    // cout<<x<<endl;

    // int a[5] = {1, 2, 3, 4, 5};  //type 5;
    // vector<int> v(a, a + 5);
    // for (int x : v)
    //     cout << x << endl;

    vector<int> v={1, 2, 3, 4, 5,6};  //type 6;
    for (int x : v)
        cout << x << endl;
    return 0;
}